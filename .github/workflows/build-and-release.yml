name: Build and Release

on:
  push:
    branches:
      - main
      - develop
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - develop

env:
  GRADLE_USER_HOME: ${{ github.workspace }}/.gradle

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build --build-cache 2>&1 | tail -50

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Build Release AAB
        continue-on-error: true
        run: ./gradlew bundleRelease 2>&1 || true

      - name: List build outputs
        if: always()
        run: |
          echo "=== Checking APK Outputs ==="
          if [ -d "app/build/outputs/apk" ]; then
            find app/build/outputs/apk -type f -name "*.apk" -exec ls -lh {} \;
          else
            echo "APK directory not found"
          fi
          
          echo "=== Checking AAB Outputs ==="
          if [ -d "app/build/outputs/bundle" ]; then
            find app/build/outputs/bundle -type f -name "*.aab" -exec ls -lh {} \;
          else
            echo "AAB directory not found"
          fi

      - name: Run unit tests
        run: ./gradlew test

      - name: Run instrumented tests
        run: ./gradlew connectedAndroidTest || true

      - name: Upload APK to artifacts
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: bookora-apk
          path: |
            app/build/outputs/apk/**/*.apk
            !app/build/outputs/apk/**/output-metadata.json

      - name: Upload AAB to artifacts
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: bookora-aab
          path: |
            app/build/outputs/bundle/**/*.aab
            !app/build/outputs/bundle/**/output-metadata.json

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: app/build/reports/

  create-release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') && needs.build.result == 'success'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download APK
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: bookora-apk

      - name: Download AAB
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: bookora-aab

      - name: List downloaded files
        run: |
          echo "=== Artifacts in current directory ===" && pwd && ls -la
          pwd
          echo "=== Files ==="
          ls -la

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Upload APK Release Asset
        if: always()
        continue-on-error: true
        uses: softprops/action-gh-release@v1
        with:
          files: |
            *.apk
          tag_name: ${{ steps.version.outputs.VERSION }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload AAB Release Asset
        if: always()
        continue-on-error: true
        uses: softprops/action-gh-release@v1
        with:
          files: |
            *.aab
          tag_name: ${{ steps.version.outputs.VERSION }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run lint
        run: ./gradlew lint || true

      - name: Upload lint reports
        uses: actions/upload-artifact@v4
        with:
          name: lint-reports
          path: app/build/reports/lint-results-debug.html

  notify:
    needs: [build, lint]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Build Status Notification
        run: |
          if [ "${{ needs.build.result }}" = "success" ] && [ "${{ needs.lint.result }}" = "success" ]; then
            echo "✅ Build and Lint SUCCESSFUL"
          else
            echo "❌ Build or Lint FAILED"
          fi
